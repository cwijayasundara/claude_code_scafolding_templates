# Required
TAVILY_API_KEY=tvly-your-api-key-here

# Required for the default LLM provider (OpenAI)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Anthropic API key (if using anthropic:* models)
# ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Optional: Override default LLM model (format: provider:model-name)
# MODEL=openai:gpt-5-mini

# Optional: Max search results per query (default: 5)
# MAX_SEARCH_RESULTS=5

# Optional: Output directory for reports (default: output)
# OUTPUT_DIR=output

# Optional: Log level (default: INFO)
# LOG_LEVEL=INFO
